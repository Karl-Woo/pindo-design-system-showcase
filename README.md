# Pindo Design System — Showcase

Auto-synced from [`qiuyichen00/pindo`](https://github.com/qiuyichen00/pindo)
(`machacha` branch, `seaFreight-showcase/` folder) on every push that
touches the showcase or its `pd-*` sources.

**Do NOT commit changes to this repo directly.** Edit the source repo
and let the `Sync showcase to design-system repo` GitHub Action mirror
the change here.

Deployed via Vercel. See `vercel.json` for the build config.
